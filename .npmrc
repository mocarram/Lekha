# Pin peer-dependency resolution for this repo: the lockfile must include
# peer chains (app-builder-lib peers on electron-builder-squirrel-windows),
# or npm ci fails on machines without legacy-peer-deps in their user config.
legacy-peer-deps=false
